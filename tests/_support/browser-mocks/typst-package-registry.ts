export function NodeFetchPackageRegistry(
  this: { accessModel: unknown; request: unknown },
  accessModel: unknown,
  request: unknown,
) {
  this.accessModel = accessModel;
  this.request = request;
}
